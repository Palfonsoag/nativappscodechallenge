import React from 'react';
import {View} from 'react-native';
import Styles from './styles';

type Props = {};

const Footer = ({}: Props) => <View style={Styles.footerContainer} />;

export default Footer;
